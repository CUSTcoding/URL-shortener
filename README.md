# URL-shortener
This project is a basic URL shortening API built using Actix(Rust), Django(Python) and Spring boot(Java). It enables users to generate short URLs for long links, track the number of clicks on each short URL, and provides redirection to the original URL.
