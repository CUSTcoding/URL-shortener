# URL Shortener

This project is a basic URL-shortening API built with **Actix Web (Rust), Django and FastAPI (Python), Gin-Gonic (Go), Fastify (Node.js), and Spring Boot (Java)**.

It enables users to generate short URLs from long links, track the number of clicks on each short URL, and redirect users to the original URL.
